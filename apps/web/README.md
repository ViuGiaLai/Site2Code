# Site2Code Web

Next.js UI for the Site2Code pipeline.

## Setup

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

- Web: http://localhost:3000
- API: http://localhost:3001 (set via `NEXT_PUBLIC_API_URL`)

Ensure `apps/api` is running before submitting a URL.
